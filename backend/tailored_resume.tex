% Base-only template: no non-standard packages, so it compiles on a minimal
% TeX install (pdflatex/xelatex with no extra .sty files).
\documentclass[11pt]{article}
\setlength{\oddsidemargin}{0pt}
\setlength{\textwidth}{6.5in}
\setlength{\topmargin}{-0.5in}
\setlength{\textheight}{9in}

\begin{document}

\section*{Candidate}

\section*{Projects}

% The PROJECTS placeholder on the line below is replaced with one block per
% project. Each block has the format:
%   \subsection*{<Project Name>}
%   \begin{itemize}
%       \item ...
%   \end{itemize}
% (The literal token must not appear in this comment, or it would be replaced too.)
\subsection*{balls}
\begin{itemize}
    \item Orchestrated complex agentic workflows by implementing state machine architectures using LangGraph and Python, reducing manual workflow intervention time by 40\%.
    \item Engineered dynamic routing and feedback loops utilizing LangChain and Google GenAI, increasing task execution accuracy by 25\% across multiple mini-projects.
    \item Streamlined conversational state management and ReAct agent patterns by integrating langchain-core, decreasing overall API response latency by 30\%.
\end{itemize}

\end{document}
